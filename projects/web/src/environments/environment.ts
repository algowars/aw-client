export const environment = {
    production: true,
    apiServerUrl: "",
    auth: {
        domain: "",
        clientId: "",
        authorizationParams: {
            redirect_uri: window.location.origin
        }
    }
};
